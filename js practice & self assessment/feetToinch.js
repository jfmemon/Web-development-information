// feet to inches
function feetToInch (feets) {
    console.log (feets + ' feet.');
    const inches = feets * 12;
    return inches;
}

const result = feetToInch (30);
console.log(result + ' inches.');